#include "point.h"
#include <vector>

#ifndef SPANNING_FACTOR_INCLUDE_HELPER_H
#define SPANNING_FACTOR_INCLUDE_HELPER_H

    double euclidean_distance(Point u, Point v);
    double perimeter(std::vector<Point> polygon, int i, int j, int k);

#endif
